<%= button_to "Home", '/welcome', method: :get%>
<%= button_to "Recieved Comments", '/welcome', method: :get%>
    <div>
        <h1><%= @user.email%> </h1>
        <h1><%= @user.username%> </h1>
    </div>

<% @user.owned_pictures.each do |picture|%> 
        <%= image_tag picture.img_url%>
        
        <% picture.tags.each do |tag| %>
            <div><%=  link_to tag.name,"/tags/#{tag.id}"%></div>
        <%end%>
<%end%>

<%#<% @user.recieved_comments.each do |comment|%>
<%#    <%= comment.comment%>
<%#<%end%>






